
const PageTitle = ({text}) => {
    return (
        <div className="page-title-container text-center py-6 sm:py-12">
            <h1 className="font-primary text-3xl sm:text-4xl font-extrabold">{text}</h1>
        </div>
    )
}

export default PageTitle